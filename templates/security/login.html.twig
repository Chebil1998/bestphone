<div class="login">
	<title>Login</title>
  <h1>Login to Bestphone</h1>
  <form method="post" action="{{path('security_login')}}">
  	<div class="form-group">
    <input type="text" placeholder="Email..." required name="_username" class="form-control"></div>
    <div class="form-group">
    <input type="password" placeholder="Password..." required name="_password" class="form-control"></div>
    <button type="submit" class="btn btn-primary btn-block btn-large">Let me in.</button>
    <div class="login-help">
  <p>Don't have an account? <a href="{{path('security_registration')}}">Sign Up</a></p>
    </div>
   </form>
</div>
<link rel="stylesheet" type="text/css" href="{{asset('css/login.css')}}">